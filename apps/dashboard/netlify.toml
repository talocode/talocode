[build]
  base = "apps/dashboard"
  command = "corepack enable && pnpm install --frozen-lockfile && pnpm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
