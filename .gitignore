node_modules/
dist/
.env
.env.local
*.log

# TypeScript project-reference build artifacts. Emitted by `tsc -b` for the
# app-level configs; regenerated on every build, never committed.
*.tsbuildinfo
apps/*/vite.config.js
apps/*/vite.config.d.ts

# Local Netlify folder
.netlify
